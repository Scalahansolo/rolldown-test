{
  "name": "rolldown-test",
  "version": "1.0.0",
  "type": "module",
  "workspaces": [
    "apps/*",
    "packages/*"
  ],
  "scripts": {
    "build": "rolldown -c",
    "build:watch": "rolldown -w -c rolldown.config.js",
    "dev": "pnpm build:watch & wait-on dist/bundle.js && node --watch dist/bundle.js",
    "fmt": "oxfmt"
  },
  "devDependencies": {
    "@types/node": "^24.8.1",
    "oxfmt": "0.6.0",
    "rolldown": "^1.0.0-beta.44",
    "tsx": "^4.20.6",
    "typescript": "^5.9.3",
    "wait-on": "9.0.1"
  },
  "dependencies": {
    "p-limit": "^7.2.0"
  }
}
